zuul:
  # pass cookies downstream using the sensitive-headers property
  sensitive-headers:
  routes:
    ui:
      path: /ui/**
      url: http://localhost:8081/ui
    admin:
      path: /admin/**
      url: http://localhost:8082/admin
    resource:
      path: /resource/**
      url: http://localhost:9000/resource